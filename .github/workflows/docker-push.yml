name: Build and Push Docker Images to AWS ECR

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-west-2

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build frontend Docker image
        run: docker build -t todo-frontend ./frontend

      - name: Tag frontend Docker image
        run: docker tag todo-frontend:latest 267092042432.dkr.ecr.us-west-2.amazonaws.com/todo-frontend:latest

      - name: Push frontend Docker image
        run: docker push 267092042432.dkr.ecr.us-west-2.amazonaws.com/todo-frontend:latest

      - name: Build backend Docker image
        run: docker build -t todo-backend ./backend

      - name: Tag backend Docker image
        run: docker tag todo-backend:latest 267092042432.dkr.ecr.us-west-2.amazonaws.com/todo-backend:latest

      - name: Push backend Docker image
        run: docker push 267092042432.dkr.ecr.us-west-2.amazonaws.com/todo-backend:latest

